[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
# Healthcheck: si la app no responde en 30s al arrancar, se reinicia
healthcheckPath = "/favicon.ico"
healthcheckTimeout = 30
